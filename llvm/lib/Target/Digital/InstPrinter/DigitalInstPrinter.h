//= DigitalInstPrinter.h - Convert Digital MCInst to asm syntax -------*- C++ -*--//
//
// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
// See https://llvm.org/LICENSE.txt for license information.
// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
//
//===----------------------------------------------------------------------===//
//
// This class prints a Digital MCInst to a .s file.
//
//===----------------------------------------------------------------------===//

#ifndef LLVM_LIB_TARGET_DIGITAL_INSTPRINTER_DIGITALINSTPRINTER_H
#define LLVM_LIB_TARGET_DIGITAL_INSTPRINTER_DIGITALINSTPRINTER_H

#include "llvm/MC/MCInstPrinter.h"
#include "MCTargetDesc/DigitalMCTargetDesc.h"

namespace llvm {
  class MCOperand;

  class DigitalInstPrinter : public MCInstPrinter {
  public:
    DigitalInstPrinter(const MCAsmInfo &MAI, const MCInstrInfo &MII,
                      const MCRegisterInfo &MRI)
      : MCInstPrinter(MAI, MII, MRI) {}

  static const char *getRegisterName(unsigned RegNo);
  void printInst(const MCInst *MI, raw_ostream &O, StringRef Annot,
                 const MCSubtargetInfo &STI) override;
  void printRegName(raw_ostream &O, unsigned RegNo) const override;

  void printOperand(const MCInst *MI, unsigned OpNo, raw_ostream &O,
                    const char *Modifier = nullptr);
  void printMemOperand(const MCInst *MI, int opNum, raw_ostream &O);


  // Autogenerated by tblgen.
  bool printAliasInstr(const MCInst *MI, raw_ostream &OS);
  void printInstruction(const MCInst *MI, raw_ostream &O);
  };
}

#endif